import React from 'react'

export const EditBallot = ({ elections }) => {
  console.log(elections)
  return (
    <>
      <h1>
        
      </h1>
      {/* {elections.find(election => election.id === 1).map(question => {
        return question.id
      })} */}
   </>
  )
}